BOT_TOKEN=123456:ABCDEF-your-token-here
ADMIN_IDS=
# Optional: comma-separated Telegram usernames (without @) who are admins
ADMIN_USERNAMES=
DATA_DIR=./data
DB_PATH=./data/fartbot.sqlite3

# Classifier modes: heuristic (default) or panns (experimental – requires torch/torchaudio)
CLASSIFIER_MODE=heuristic

# Heuristic tuning (empirical defaults; tweak if needed)
HEURISTIC_LOWFREQ_RATIO=1.35
HEURISTIC_ROLLOFF_MAX=1600
HEURISTIC_ZCR_MAX=0.12
MIN_VOICE_SECONDS=1
MAX_VOICE_SECONDS=12
CONFIDENCE_MIN=0.55
